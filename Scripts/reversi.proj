<?xml version="1.0" encoding="utf-8"?>
<Project ToolsVersion="4.0" DefaultTargets="Build"
         xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <SolutionDir>$(MSBuildProjectDirectory)\..</SolutionDir>
    <SolutionFile>$(SolutionDir)\reversi.sln</SolutionFile>
    <Configuration>Release</Configuration>
    <TargetFramework>net471</TargetFramework>
    <Platform>Any CPU</Platform>
  </PropertyGroup>

  <Target Name="RestorePackages">
    <Exec Command="dotnet restore &quot;$(SolutionFile)&quot; /p:Platform=&quot;$(Platform)&quot;" />
  </Target>

  <Target Name="Build" DependsOnTargets="RestorePackages">
    <MSBuild Projects="$(SolutionFile)" Targets="Build" Properties="Configuration=$(Configuration);TargetFramework=$(TargetFramework);Platform=$(Platform)" />
  </Target>

  <Target Name="Clean">
    <MSBuild Projects="$(SolutionFile)" Targets="Clean" Properties="Configuration=$(Configuration);TargetFramework=$(TargetFramework);Platform=$(Platform)" />
  </Target>

  <Target Name="UnitTest">
    <Exec Command="nunit3-console --result=TestResult_$(Configuration).xml $(SolutionDir)\UnitTests\bin\$(Configuration)\$(TargetFramework)\reversi.tests.dll" />
  </Target>

  <Target Name="All" DependsOnTargets="Clean; Build; UnitTest" />

</Project>
